# chameleon（变色龙）— Chrome 会话隔离管理工具

面向国内手工测试团队的桌面工具：每个测试角色运行在独立的 Chrome 用户数据目录中，通过 CDP 受工具控制，支持会话接力、快照与沙箱。纯本地、离线、无谷歌账号依赖。变色龙 = 随身份变色的窗口，每个角色窗口配一个色块，一眼可辨。

## Language

**角色 (Role)**:
命名隔离单元 = 名称 + 颜色 + 数据目录 + CDP 端口 + 常用 URL 列表。对应一个独立的 Chrome 实例。
_Avoid_: 配置、账号、环境、窗口

**数据目录 (ProfileDir)**:
角色专属的 Chrome `--user-data-dir`，与 Chrome/Edge 默认配置目录严格互斥。
_Avoid_: 配置目录、profile 文件夹

**接力 (Handoff)**:
会话接力：把角色 A 当前激活标签页的 URL 传递到角色 B 的隔离窗口并以新标签页打开。非苹果/AI 领域的 Handoff。
_Avoid_: 跳转、转发、切换

**并行模式 (Parallel Mode)**:
接力时保留原角色窗口，双窗口并排对比权限差异。
_Avoid_: 对比模式

**接力模式 (Relay Mode)**:
接力时通过 CDP 优雅关闭原角色窗口，仅保留目标角色窗口。
_Avoid_: 替换模式

**会话快照 (Snapshot)**:
某一时刻所有角色的打开标签页 URL 与窗口位置的 JSON 记录，可恢复。
_Avoid_: 存档、备份

**临时沙箱 (Sandbox)**:
用完即删的一次性隔离窗口，进程退出后自动删除临时数据目录。
_Avoid_: 临时窗口、无痕窗口

**常用 URL 预设 (Quick Links)**:
角色预定义的常用测试地址，点击即在新标签页打开。
_Avoid_: 收藏夹、书签

**一键启动 / 一键关闭**:
批量操作：启动所有预定义角色窗口 / 通过 CDP 优雅关闭所有测试用 Chrome 窗口。仅作用于测试数据目录，绝不触碰 Chrome 默认配置。
_Avoid_: 批量命令、全关