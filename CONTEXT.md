# chameleon（变色龙）— Chrome 会话隔离管理工具

面向国内手工测试团队的桌面工具：每个测试角色运行在独立的 Chrome 用户数据目录中，通过 CDP 受工具控制，支持会话接力、快照与沙箱。纯本地、离线、无谷歌账号依赖。变色龙 = 随身份变色的窗口，每个角色窗口配一个色块，一眼可辨。

## Language

**系统 (System)**:
被测应用的命名容器，含多个角色；角色可选归属一个系统。系统级常用 URL 预设由组内所有角色共享。启动组 = 启动该系统下全部角色。删除系统 = 解除其角色的归属（角色保留、变为未分组），不级联删除角色。
_Avoid_: 组、项目、应用

**角色 (Role)**:
命名隔离单元 = 名称 + 颜色 + 数据目录 + CDP 端口 + 常用 URL 预设。对应一个独立的 Chrome 实例。可选属于一个系统；各角色数据目录与登录态严格隔离。
_Avoid_: 配置、账号、环境、窗口

**数据目录 (ProfileDir)**:
角色专属的 Chrome `--user-data-dir`，与 Chrome/Edge 默认配置目录严格互斥。
_Avoid_: 配置目录、profile 文件夹

**登录辅助 (Login Assist)**:
挂在角色级常用 URL 预设上的全自动登录凭据：用户名 + 密码 + 输入框选择器（可选，藏在高级区）。点该预设即打开 URL 并自动填用户名/密码。一个角色可挂多个登录辅助（对应不同测试账号）。存储密码——纯本地离线工具，风险等同 Chrome 记住密码。仅角色级预设可挂；系统级预设共享 URL 不共享身份。
_Avoid_: 角色级登录辅助、半自动登录、密码管理、凭证存储

**接力 (Handoff)**:
会话接力：把角色 A 当前激活标签页的 URL 传递到角色 B 的隔离窗口并以新标签页打开。非苹果/AI 领域的 Handoff。
_Avoid_: 跳转、转发、切换

**并行模式 (Parallel Mode)**:
接力时保留原角色窗口，双窗口并排对比权限差异。
_Avoid_: 对比模式

**接力模式 (Relay Mode)**:
接力时通过 CDP 优雅关闭原角色窗口，仅保留目标角色窗口。
_Avoid_: 替换模式

**会话快照 (Snapshot)**:
某一时刻各角色打开的测试标签页 URL 与窗口位置的 JSON 记录。恢复 = 对齐快照内角色的标签页与窗口位置；快照外的角色与沙箱不受影响、不被关闭。仅记录测试标签页，不含 chameleon 内部占位页签。按单窗口角色建模。
_Avoid_: 存档、备份、整体状态回滚

**临时沙箱 (Sandbox)**:
用完即删的一次性隔离窗口，进程退出后自动删除临时数据目录。与角色/系统无关、可多开、无名；不纳入快照、不记忆窗口位置。
_Avoid_: 临时窗口、无痕窗口

**常用 URL 预设 (Quick Links)**:
命名 URL 书签 = 名称 + 地址，内部以 id 为唯一键。点击即在角色隔离窗口新标签页打开（角色未启动则自动启动再打开）。系统级预设 = 组内所有角色实时共享的预设（组内任一角色可直接点开，无需拷贝）；角色级预设 = 角色私有。角色级预设可挂登录辅助；系统级预设不可。启动时自动打开是预设的正交标记，系统级预设的该标记对组内所有角色生效。启动时不按 URL 去重——用户勾选即执行。
_Avoid_: 收藏夹、书签

**一键启动 / 一键关闭 / 启动组**:
批量操作：启动所有预定义角色窗口 / 通过 CDP 优雅关闭所有角色窗口与沙箱窗口 / 启动某系统下全部角色。仅作用于测试数据目录，绝不触碰 Chrome 默认配置。
_Avoid_: 批量命令、全关

**UI 偏好 (UiPreferences)**:
用户界面设置：主题模式（深色/浅色/跟随系统）+ 面板透明度（0.5-1.0）+ Accent 颜色。持久化到 config.json 的 ui_preferences 字段，跨重启保留。随配置导出/导入同行。
_Avoid_: 外观设置、主题配置、个性化

**Hybrid 主题 (Hybrid Theme)**:
chameleon 的主题材质策略：深色模式 = Windows 原生 Mica (Win11) / Acrylic (Win10) 半透背景 + 半透面板；浅色模式 = 完全关闭 vibrancy，由前端实色浅色背景（#F3F3F3）+ 实色面板填充。参考 Windows 11 自家设置应用的做法。Rust 端 `set_ui_preferences` 在主题切换时同步调用 `clear_vibrancy` + 重新应用 Mica/Acrylic。
_Avoid_: 全程云母、全程实色、自适应 Mica
