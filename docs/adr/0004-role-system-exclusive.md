# 角色系统专属，不跨系统复用登录态

角色挂 `system_id` 可选归属一个系统；同一角色不跨系统复用。被测系统的登录态（Cookie/Session）受域隔离约束，不同系统无法共享 Chrome 用户数据目录的登录态——「跨系统复用同一身份」是物理错觉，强行复用会制造跨系统串号。系统专属 = 每个角色独立数据目录，安全边界清晰，与「数据目录严格互斥」原则一致。

**Considered Options**: 角色与系统多对多（同一身份跨系统复用数据目录）——否决，因登录态物理隔离，复用是错觉且引入串号风险。
