# 07 — 配置导出/导入

**What to build:** 导出角色配置为明文文件；导入配置（新人入职复用），导入时校验默认目录拒绝与端口冲突，冲突时给出中文提示且不破坏现有配置。

**Blocked by:** 01 — 骨架 + 角色创建 + 首个隔离窗口

**Status:** ready-for-agent

- [ ] 导出配置为明文可读文件
- [ ] 导入后角色完整可用（名称/颜色/预设/端口）
- [ ] 导入含默认配置目录或端口冲突的配置时给出中文提示，且不破坏现有配置