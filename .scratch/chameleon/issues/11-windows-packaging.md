# 11 — Windows 打包发布

**What to build:** mingw 交叉编译 Windows 单 exe；便携文件夹布局（exe + 配置 + 数据根一个文件夹可搬运）；缺少 WebView2 运行时给出中文安装提示而非白屏。

**Blocked by:** 01 — 骨架 + 角色创建 + 首个隔离窗口；02 — 浏览器检测兜底 + 手动指定 + 单实例锁 + 自然语言错误层；03 — Handoff 会话接力；04 — 一键启动/关闭所有角色；05 — 常用 URL 预设；06 — 窗口位置记忆；07 — 配置导出/导入

**Status:** ready-for-agent

- [ ] Windows 机器上单 exe 可运行全部 v1 功能
- [ ] 整个工具一个文件夹可搬运，配置与数据随行
- [ ] 缺 WebView2 运行时给出中文安装提示而非白屏